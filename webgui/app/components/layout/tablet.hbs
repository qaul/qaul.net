<div local-class="root">
  <div aria-hidden="true" local-class="topbar-main">
    <button {{on "click" this.slideLogIn}}>
      {{fa-icon "history"}}
    </button>
  </div>
  <Layout::TabletNav local-class="nav" />
  <Layout::SlideLog local-class="log" @showLog={{this.showLog}} @slideLogOut={{this.slideLogOut}}  />
  <main local-class="main">{{yield}}</main>
</div>