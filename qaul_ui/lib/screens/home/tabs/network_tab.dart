part of '../home_screen.dart';

class _NetworkTab extends StatelessWidget {
  const _NetworkTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Network Tab'));
  }
}
