part of '../home_screen.dart';

class _ChatTab extends StatelessWidget {
  const _ChatTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chat Tab'));
  }
}
