import ApplicationSerializer from './application';

export default class ChatRoomSerializer extends ApplicationSerializer {
  // normalizeCreateRecordResponse(store, primaryModelClass, payload) {
  //   const dat = {
  //     data: { id: payload.room_id[0], type: primaryModelClass.modelName }
  //   };

  //   return dat;
  // }
}
