description: Pin dependency versions to be compatible with the CI rustc version
steps:
  - run:
      name: Pin dependencies for rustc compatibility
      command: |
        cd rust
        cargo update time --precise 0.3.45
        cargo update time-core --precise 0.1.7
        cargo update time-macros --precise 0.2.25
